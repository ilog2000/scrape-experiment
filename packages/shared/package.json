{
  "name": "@scrape-experiment/shared",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build:types": "tsc --build",
    "build": "npm-run-all --parallel build:types",
    "lint:eslint": "eslint ./src --ext js,ts,tsx --max-warnings 0",
    "lint": "npm-run-all --parallel lint:eslint",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/pg": "^8.10.9"
  }
}
